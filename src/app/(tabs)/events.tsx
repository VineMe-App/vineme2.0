import React from 'react';
import { View } from 'react-native';

// Events page - hidden for now, keeping file for future use
export default function EventsScreen() {
  // Return empty view so page doesn't show anything if navigated to directly
  return <View style={{ flex: 1 }} />;
}
